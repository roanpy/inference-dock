schema_version: 1
id: vllm-mlx
display_name: vllm-mlx
discover:
  # Console script name is a discovery hint only; absence just reports
  # not_found and never implies the project is unusable.
  executables: ["vllm-mlx"]
service:
  # Ports and lifecycle are user-chosen per launch; no endpoint, activation,
  # or probe is claimed until a fixed-version install is verified.
  mode: managed
  service_key: vllm-mlx-server
  protocol: openai-chat
models_source: none
repositories:
  - remote: https://github.com/waybarrios/vllm-mlx
