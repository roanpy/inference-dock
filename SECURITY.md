# Security Policy

InferenceDock 当前是 loopback-only Developer Preview，不是可直接暴露到局域网或公网的网关。默认监听 `127.0.0.1`，没有完整认证层；不要把监听地址改成 `0.0.0.0`。

## 报告问题

请不要在公开 issue 中粘贴提示、图片、工具结果、模型路径、凭据或完整私有日志。优先向仓库维护者发送最小复现、版本/提交、脱敏配置、影响和安全边界。未建立私密接收地址前，先开一个不含敏感数据的 issue，请维护者提供联系渠道。

## 发布安全

- 公开打包必须显式使用仓库内的 `config/engines.example.yaml` 或其他审查过的公共配置。
- `scripts/export_source.py` 会拒绝个人路径、明显凭据、日志/缓存目录和真实模型工件。
- 第三方服务器和模型不由本项目下载、更新、接管或分发。
- 回滚只能停止由 InferenceDock 明确拥有的进程；不得删除用户配置或权重。

## 已知限制

外部服务的活动状态若没有可靠探针，就不能证明可安全卸载或切换。资源组容量是调度约束，不是 RAM、Metal 或 swap 安全证明。发现和配置声明也不是引擎能力验证。

